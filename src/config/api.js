const API_ROOT = '/api'
const type = {
    POST: 'post',
    GET: 'get'
}

const config = {
    center: {
        // test
        getTest: {
            url: API_ROOT + '/www.ddy98123.com',
            type: type.GET
        }
    }
}

export default config
